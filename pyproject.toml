[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pacsim"
readme = "README.md"
requires-python = ">=3.10"
version = "1.0.0"
dependencies = [
    "numpy >= 1.26",
    "openmm >= 8.1",
    "pytest >= 7.4",
    "gsd >= 3.2",
    "pyyaml >= 6.0",
    "tqdm >= 4.65",
    "pandas >= 2.2",
    "ase >= 3.22",
    "scipy >= 1.12",
    "freud-analysis >= 3.3",
    "pymatgen >= 2025.10"
]

[project.scripts]
pacsim-run = "colloids.colloids_run:main"
pacsim-create = "colloids.colloids_create.colloids_create:main"

[tool.setuptools.packages.find]
where = ["."]